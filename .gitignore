# Worker/orchestration artifacts — must NOT land in the public repo
CLAUDE.md
send_plan.json
.claude/

# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# Build artifacts (deploy zips are generated, not committed)
dist/

# Exporter runtime output (live awaiting_user notes — must NOT be committed)
.state/
